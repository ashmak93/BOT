version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: text-analysis-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - GIGACHAT_TOKEN=${GIGACHAT_TOKEN}